# twitoff- name of inner folder. APP- name of variable in __init__.py
# -t 120 is to wait 2 minutes for it to load before timing out- only bc
# we're using free version of heroku
web: gunicorn twitoff:APP -t 120